{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview",
    "android": "npm run build && npx cap sync && npx cap run android",
    "live-reload": "npx cap sync && npx cap open android",
    "adb:start": "adb start-server",
    "adb:devices": "adb devices"
  },
  "dependencies": {
    "@capacitor/android": "^8.2.0",
    "@capacitor/core": "^8.2.0",
    "@capacitor/ios": "^8.2.0",
    "@element-plus/icons-vue": "^2.3.2",
    "axios": "^1.13.6",
    "echarts": "^6.0.0",
    "element-plus": "^2.13.5",
    "vue": "^3.5.12",
    "vue-echarts": "^8.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.2.0",
    "@vitejs/plugin-vue": "^5.1.4",
    "typescript": "~5.6.2",
    "vite": "^5.4.10",
    "vue-tsc": "^2.1.8"
  }
}
